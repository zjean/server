node_modules
logs
.run
dist
release
.idea
.DS_Store
coverage
backend/coverage
backend/logs
environment/environment.yaml
frontend/static
frontend/.angular/
frontend/tmp
frontend/out-tsc
frontend/bazel-out
/frontend/src/assets/pdfjs/
circular-dependencies.png
backend/src/applications/files/assets/ocr-languages/*.traineddata
.husky/_/

# fork-local: runtime user data written by the dev stack (avatars, uploaded files).
# Upstream tracks nothing under data/; without this, a blanket `git add` during a
# sync-merge conflict commits real user content to a public repo.
data/
# fork-local: machine-specific Claude Code state (absolute paths, local usernames)
.claude/settings.local.json
.claude/scheduled_tasks.lock